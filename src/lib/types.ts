export type Variant = { id: string; sku: string; size: string | null; color: string; stock: number; price: number | null };
export type Product = { id: string; name: string; slug: string; description: string; short_description: string; category: 'shoes' | 'watches'; brand: string; base_price: number; compare_at_price: number | null; sku: string; featured: boolean; new_arrival: boolean; best_seller: boolean; active: boolean; primary_image: string; images: string[]; specifications: Record<string,string>; variants: Variant[]; created_at: string };
export type CartItem = { variantId: string; productId: string; name: string; slug: string; image: string; size: string | null; color: string; price: number; quantity: number; stock: number };
export type Quote = { items: CartItem[]; subtotal: number; delivery: number; total: number; fingerprint: string };
export type Order = { order_number: string; customer_name: string; phone: string; address: string; city: string; province: string; subtotal: number; delivery_charge: number; total: number; order_status: string; created_at: string; order_items: { product_name: string; sku: string; size: string | null; color: string; unit_price: number; quantity: number; line_total: number; primary_image: string }[] };
